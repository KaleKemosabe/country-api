import React from 'react';
import './Hero.css';

const Hero = () => {

    return (
        <main className="hero">
            <h1 className="hero-title">What's going on?</h1>
            <p className="hero-text">Europe's country details and main news</p>
        </main>
    );
  };
  
  export default Hero;